package Google;

import Project.Base;
import org.openqa.selenium.WebDriver;

public class GoogleSearchPage extends GoogleGeneral {

    public GoogleSearchPage(WebDriver driver) {
        super(driver);
    }

    public void searchLinkAndClickOne(String text) {
        super.searchLinkAndClickOne(text);
    }
}